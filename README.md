# ⚠️ DEPRECATED — this repository is no longer maintained

**Retired 2026-08-13.** The `_contents/` here has been absorbed into the active content vault, **[`content-works`](https://github.com/skyfish-growingwithai/content-works)**:

| Moved from (`_contents/`) | Now lives in (`content-works`) |
|---|---|
| Curriculum — `000-Overview`, `101-Intro-AI-Tools`, `201`–`501`, `intro.md` | `10-teaching-inventory/` |
| Business docs — `business_docs_folder/`, `freelance-*.md`, `솔테티 제안서` | `40-artifacts(산출물)/business/` |

➡️ **All new content work happens in [`content-works`](https://github.com/skyfish-growingwithai/content-works). Do not author or update anything here.**

This repository is kept only as a historical reference.
